LLM_URL = "https://generativelanguage.googleapis.com/v1beta/openai/"
LLM_API_KEY = ""  # insert your api key

model_name = "gemini-3.1-flash-lite"

EXPORT_DIR = "./exports"

# populate following as per your DB config. 
# Since following credentials are not sensitive because of mock DB, including values.
db_host = "postgres"
db_port = "5432"
db_name = "banking_db"
db_user = "manager"
db_password = "postgres"